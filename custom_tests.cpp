#include <vector>
#include <string>
#include <map>

using namespace std;

typedef pair<string, string> pss;

// TODO: maybe it'd better to generate this thing in python script instead of files. this would allow for easy tests fixes

vector<pss> get_custom_tests(string problem)
{
    vector<pss> tests;

    switch (problem[0]) {
//  EXAMPLE
//      tests.push_back(pss("1 0 0.3", "0.300"));
    case 'A':

        break;

    case 'B':

        break;

    case 'C':

        break;

    case 'D':

        break;

    case 'E':

        break;

    case 'F':

        break;
    }
    return tests;
}
